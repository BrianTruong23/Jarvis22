# === Required ===
GITHUB_TOKEN=ghp_your_token_here
TARGET_REPO=owner/repo1,owner/repo2   # Comma-separated, watches all repos

# === Optional ===
# ANTHROPIC_API_KEY=sk-ant-...      # Only needed if NOT using `claude login` (Claude Plus OAuth)
# POLL_INTERVAL=60                  # Seconds between polls
# ISSUE_LABEL=jarvis                # Label to watch for
# READY_LABEL=ready                 # Issues need both ISSUE_LABEL + READY_LABEL
# DONE_LABEL=jarvis-done            # Label added on completion
# WORKSPACE_DIR=/tmp/jarvis-workspace
# DB_PATH=jarvis.db
# BRANCH_PREFIX=jarvis/issue-
# CLAUDE_MODEL=sonnet               # Model for Claude Code CLI
# CLAUDE_MAX_BUDGET=5.00            # Max USD per run
# WEBHOOK_PORT=8080
# WEBHOOK_SECRET=                   # GitHub webhook secret for HMAC verification
# LOG_LEVEL=INFO

# === Session & Guardrails ===
# SESSION_TIMEOUT=7200              # 2h total session timeout (seconds)
# ISSUE_TIMEOUT=1800                # 30 min per issue timeout (seconds)
# MAX_DIFF_FILES=40                 # Max files changed per issue
# MAX_DIFF_LOC=1000                 # Max lines of code changed per issue
# MAX_TOKENS_PER_RUN=180000         # Max tokens for entire session
# TOKEN_WARNING_BUFFER=5000         # Stop when within this many tokens of limit

# === Codex Fallback ===
# CODEX_BINARY=node /usr/lib/node_modules/@openai/codex/bin/codex.js
# CODEX_MODEL=o4-mini               # Model for Codex fallback

# === Reports ===
# REPORTS_DIR=reports               # Directory for report files
# JARVIS_REPO_DIR=                  # Path to Jarvis22 repo for committing reports
# PUBLISH=false                     # Set to true to allow posting to external platforms
